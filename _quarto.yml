project:
  type: manuscript
  output-dir: .           # Render to the repo root instead of _output
  resources:
    - "_*.qmd"

manuscript:
  article: capstone.qmd

format: 
  html:
    output-file: index    # Render as index.html
    theme:
      light: lightly
      dark: darkly
